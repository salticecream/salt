# (A copy of) the prelude for the salt language
# All of these functions are in the global scope without any imports necessary
extern fn print(char* str) -> int
extern fn scan(char* buf, usize count) -> int